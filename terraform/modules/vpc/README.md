# VPC Module

This module contains the existing VPC infrastructure configuration used for network isolation across the event-driven architecture.

## Expected outputs

- `vpc_id`
- `private_subnet_ids`
- `public_subnet_ids`
- `security_group_ids`
